<%= t(:reported_problem) %>: <%= @report.description %>

Dentro de alguns dias iremos marcar este problema como resolvido.
Caso ainda não esteja resolvido, podes usar este link para extender o tempo: <%= edit_report_path(@report, :token => @report.token) %> .
